import { Grid } from '@mui/material';
import React, { FC } from 'react'

type Props = {}

const podcastplayer :FC= (props: Props) => {
  return (
   <Grid container>

    
   </Grid>
  )
}
export default podcastplayer;